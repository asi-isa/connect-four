<template>
  <div class="flex flex-wrap gap-1 w-10">
    <div
      class="w-4 aspect-square rounded-full bg-[var(--accent-yellow)] shadow-2xl border-2 border-[var(--black)]"
    />

    <div
      class="w-4 aspect-square rounded-full bg-[var(--accent-red)] shadow-2xl border-2 border-[var(--black)]"
    />

    <div
      class="w-4 aspect-square rounded-full bg-[var(--accent-red)] shadow-2xl border-2 border-[var(--black)]"
    />

    <div
      class="w-4 aspect-square rounded-full bg-[var(--accent-yellow)] shadow-2xl border-2 border-[var(--black)]"
    />
  </div>
</template>
